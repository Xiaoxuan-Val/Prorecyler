doctype html
html
    head
        title AllTrash
        link(rel='stylesheet', href='/css/trash_index.css')
    body
        #header
        each trash in trashs
            a(href=`./trashs/${trash.id}`)
                #box #{trash.name}